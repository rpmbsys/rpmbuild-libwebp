version: "3.4"
services:
  centos8build:
    volumes:
      - rpms:/home/centos/rpmbuild/SRPMS
      - rpm8:/home/centos/rpmbuild/RPMS
    build:
      context: .
      args:
        centos: $OS8

volumes:
  rpms:
    name: rpms
  rpm8:
    name: rpm8
